#include "main.h"

/**
 * mul - check for multiple character
 * @a: -The first integer to be checked
 * @b: -The second initeger to be checked
 * Return: Always 0 (sucess)
 */

int mul(int a, int b)
{
	return (a * b);
}
